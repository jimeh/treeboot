# Treeboot platform package

This package contains one platform-specific Treeboot executable. Do not install
it directly. Install the `treeboot` package, which selects the correct optional
dependency for the current platform and architecture.
